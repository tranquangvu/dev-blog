= form_for [article, article.comments.build] do |f|
  = f.text_field :content, placeholder: "Type comment content...", required: true, class: "form-control"
  = f.submit "Add Comment", class: "btn btn-main float-right"